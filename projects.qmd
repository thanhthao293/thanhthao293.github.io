---
pagetitle: Thao Nguyen - Projects
subtitle: "A collection of both my academic and personal projects"
listing: 
  contents: projects # this name must match the name of the folder you created in step #1; here, all Quarto docs in the `posts` directory will be included on your listing page
  categories: true # allows you to sort posts by assigned categories
  max-description-length: 175
  fields: [image, date, title, author]
  sort: "date desc" # can also sort on more than one field
  sort-ui: true
  type: grid # or `default` or `table`; each type has its own set of yaml options to include
---
